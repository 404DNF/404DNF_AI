# 🧠 Neuro-Symbolic Dark Pattern Detection (404DNF)

> **DeepProbLog 기반 다크패턴 탐지 시스템**
>
> 404DNF는 웹사이트의 다크패턴을 자동 탐지하기 위해 **ResGCN (Neural)** 과 **Attention 기반 Ontology 규칙 (Symbolic)** 을 결합한 **Neuro-Symbolic AI 모델**입니다.  
> 데이터 기반 학습과 논리 기반 추론을 동시에 수행하여, 기존 방법보다 높은 **해석 가능성(Explainability)** 과 **일반화 능력(Generalizability)** 을 목표로 합니다.

---

## 📋 프로젝트 개요

본 프로젝트는 웹 UI 텍스트 내에 숨겨진 **Dark Pattern**을 자동으로 탐지하기 위해 설계되었습니다.  
선행연구의 데이터셋(3,200개)을 기반으로, 각 문장은 다음의 세 요소를 포함합니다:

- **Type** : 다크패턴의 상위 유형 (예: 긴급성, 조건부 접근, 기만 등)  
- **Predicate** : Type보다 세부적인 행위/패턴 단위  
- **Label** : 다크패턴 여부 (binary)

이 데이터를 바탕으로 **ResGCN 모델링**과 **Attention 기반 규칙 추출**, 그리고 **DeepProbLog 통합 추론**을 진행했습니다.

---

## 🧩 모델링 파이프라인

### 1️⃣ ResGCN (Neural Model)
- **목적:** 문장 내 의미적 연결 관계를 학습하여 다크패턴의 Type 분류  
- **입력:** 전처리된 문장(웹사이트 문구, UI 텍스트 등)  
- **출력:** 다크패턴 유형(Type)  
- **특징 및 장점:**
  - 문장 내 토큰 간 관계를 학습하는 **그래프 구조 학습(ResGCN)**  
  - BERT와 유사한 성능, **경량 모델로 효율적 학습 및 추론 가능**  
  - 데이터가 3천여 개로 적은 환경에서도 **안정적인 성능 유지**

---

### 2️⃣ Attention 기반 Ontology 규칙 구성 (Symbolic Knowledge Extraction)

#### (1) Type 개별 규칙  
- ResGCN이 Type 분류 시 **가장 주목한 토큰 집합**을 Attention으로 추출  
- 예: `"지금 결제하지 않으면 혜택이 사라집니다"` → {‘지금’, ‘사라집니다’} → `"긴급성"` 규칙 생성  

#### (2) Predicate 개별 규칙  
- 문장의 **행위(Action)** 중심으로 predicate 분류  
- 예: `"가입하지 않으면 접근 불가"` → {‘가입’, ‘접근 불가’} → `"조건적 접근 제한"` 규칙 생성  

#### (3) Type ↔ Predicate 관계 규칙  
- Type과 Predicate 간의 **의미적 연관성**을 기반으로 관계 규칙 도출  
- 예: `"긴급성(Type)" ↔ "시간 제약(Predicate)"`  
- **기존 논문에서 제시되지 않은 새로운 규칙 관계 탐색 가능**

🧠 **장점**
- Attention 기반 규칙은 **모델 해석력(Explainability)** 을 제공  
- Symbolic 규칙은 **추론 가능한 구조(Ontology)** 로 확장 가능  
- 새로운 규칙을 학습 데이터 외부에서 **지식 기반으로 추가 가능**

---

### 3️⃣ DeepProbLog (Neural + Symbolic 통합)

| 구성 요소 | 설명 |
|------------|------|
| **Neural 부분** | ResGCN의 예측 확률(`p(Type|text)`, `p(Predicate|text)`) |
| **Symbolic 부분** | Attention으로 생성된 `.pl` 파일 (Ontology 규칙 집합) |
| **결합 구조** | Neural predicate를 통해 확률적 논리 추론 수행 |

- **DeepProbLog**는 **Neural Network와 Logic Program을 통합**하는 프레임워크로,  
  본 연구에서는 ResGCN의 출력을 Neural predicate로 활용하고,  
  Attention 기반 규칙을 PL 형식으로 연결하여 **확률적 추론 모델**을 구성했습니다.

💡 **장점**
- **데이터 기반 학습 + 규칙 기반 reasoning** 결합  
- 모델의 **신뢰성 및 설명 가능성 향상**  
- Type–Predicate–Context 간 **복합 관계 추론 가능**

---

## 🧠 전체 파이프라인 요약

```mermaid
flowchart TD
    A[Input Text] --> B[ResGCN (Neural Model)]
    B --> C[Type / Predicate Classification]
    C --> D[Attention-based Token Extraction]
    D --> E[Ontology Rule Generation (.pl)]
    E --> F[DeepProbLog Integration]
    F --> G[Final Probabilistic Inference]

    classDef step fill:#edf6f9,stroke:#023047,stroke-width:2px;
    class A,B,C,D,E,F,G step;
